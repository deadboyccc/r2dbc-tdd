spring:
  jmx:
    enabled: true
  sql:
    init:
      mode: always
  application:
    name: r2dbc-tdd
  security:
    user:
      name: u
      password: p
#  boot:
#    admin:
#      client:
#        url: http://localhost:9090
#        username: u
#        password: p

management:
  health:
    probes:
      enabled: true
  info:
    env:
      enabled: true
  endpoints:
    jmx:
      exposure:
        include: "*"
    web:
      exposure:
        include: "*"

info:
  contact:
    name: R2DBC TDD Demo
    email: random@r@c.om
    phone: 1234567890
  app:
    name: r2dbc-tdd
    description: A demo application for R2DBC and TDD
    version: 1.0.0

server:
  shutdown: immediate
logging:
  level:
    root: INFO
    dev.dead: TRACE